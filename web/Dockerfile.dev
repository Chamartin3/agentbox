FROM node:20-alpine

WORKDIR /web
COPY package.json ./
RUN npm install --no-audit --no-fund

# Source mounted as a volume by docker-compose so edits propagate.
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
