"""Feedback data helpers.

Run/usage analytics queries moved to the Data-layer managers
(``RunManager``/``UsageManager``) per CORE_ARCHITECTURE.md. This package now
holds only the remaining feedback helpers (snapshots, types, profile stats).
"""
