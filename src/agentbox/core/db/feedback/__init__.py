"""Feedback data helpers.

Aggregate queries for run/usage/profile analytics have moved to the
Data-layer managers (``RunManager``, ``UsageManager``,
``RunnerProfileManager``) per CORE_ARCHITECTURE.md.

This package now holds only ``types.py`` (``ActivityRange`` / ``since_iso``).
"""
