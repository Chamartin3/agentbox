"""Residual composition package: only the ``drift`` and ``prompts`` submodules
remain here (imported directly as ``composition.drift`` / ``composition.prompts``);
prompt composition moved to ``core.agents.prompt``. Phase D relocates these two
into ``core.agents.versioning`` and removes this package entirely.
"""
