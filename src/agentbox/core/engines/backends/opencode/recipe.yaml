engine: opencode

layout:
  context: AGENTS.md
  engine_config: opencode.json
  subagent: .opencode/agents/{name}.md

serialization:
  engine_config: json
  subagent: raw

templates:
  subagent: templates/subagent.md.tmpl
