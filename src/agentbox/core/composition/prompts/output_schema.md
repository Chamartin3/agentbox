# Required Output

Respond with a SINGLE JSON object that strictly conforms to the schema below. Output ONLY the JSON object — no prose, no markdown fences, no commentary before or after.

## JSON Schema

{schema}
