"""Workspace tooling — servers (install / list / talk). ZERO policy/grant code.

Answers "what MCP servers/tools exist", never "who may". Grant/permission
resolution lives in ``core/tools``; agent∩catalog intersection at dispatch.
"""
